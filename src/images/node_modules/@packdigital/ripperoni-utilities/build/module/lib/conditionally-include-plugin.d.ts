/**
 *
 * @export
 * @param {string} id
 * @returns {string}
 */
export declare function conditionallyIncludePlugin({ resolve, options, theme, requiredOptions, previewable, enabled: isEnabled, }: ConditionallyIncludePluginArguments): GatsbyPlugin[];
export interface GatsbyPlugin {
    resolve: string;
    options: PluginOptions;
}
export interface PluginOptions {
    [key: string]: object;
}
export interface ConditionallyIncludePluginArguments {
    resolve: string;
    options: PluginOptions;
    theme?: string;
    requiredOptions?: string[];
    previewable?: boolean;
    enabled?: boolean;
}
