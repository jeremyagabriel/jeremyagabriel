import Options from '../Options/Options';
export default class Fonts {
    private readonly options;
    private readonly format;
    constructor(format: string, options?: Options);
    createRequestUrls: () => string[];
    requestFont: (requestUrl: string, encoding: string) => Promise<string>;
    requestFontsCSS: () => Promise<string>;
    requestFontFiles: (fontUrls: string[]) => Promise<string[]>;
    requestFontFile: (fontUrl: string) => Promise<string>;
    encode: (css: string) => Promise<string>;
}
