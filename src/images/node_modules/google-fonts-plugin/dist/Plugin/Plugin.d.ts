import { compilation as webpackCompilation, Compiler } from 'webpack';
import DefaultOptions from '../Options/DefaultOptions';
export default class Plugin {
    private static pluginName;
    private readonly options;
    constructor(input?: DefaultOptions | string);
    static getPluginName: () => string;
    getFilename: (format: string, compilation: webpackCompilation.Compilation) => string;
    apply: (compiler: Compiler) => void;
}
