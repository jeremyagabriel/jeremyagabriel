import Options from '../Options/Options';
export default class Stats {
    private static UA_CODE;
    private static HOST;
    private static PATH;
    private readonly clientId;
    constructor();
    track: (options: Options) => Promise<boolean>;
}
